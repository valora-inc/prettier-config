# prettier-config

Valora's Prettier configuration.

## Usage

**Install**:

```
yarn add --dev @valora/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@valora/prettier-config",
}
```
